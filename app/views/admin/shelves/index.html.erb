<h1>Listing shelves</h1>

<table id="example">
  <thead>
    <tr>
      <th>Label</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @shelves.each do |shelf| %>
      <tr>
        <td><%= shelf.label %></td>
        <td><%= link_to 'books' , admin_shelf_books_path(shelf) %></td>
        <td><%= link_to 'Edit', edit_admin_shelf_path(shelf) %></td>
        <td><%= link_to 'Destroy', admin_shelf_path(shelf) , method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Shelf', new_admin_shelf_path %>
